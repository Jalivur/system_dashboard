# `config.__init__`

> **Ruta**: `config/__init__.py`

Paquete de configuración

## Imports

```python
from .settings import PROJECT_ROOT, DATA_DIR, SCRIPTS_DIR, STATE_FILE, CURVE_FILE, DSI_WIDTH, DSI_HEIGHT, DSI_X, DSI_Y, UPDATE_MS, HISTORY, GRAPH_WIDTH, GRAPH_HEIGHT, CPU_WARN, CPU_CRIT, TEMP_WARN, TEMP_CRIT, RAM_WARN, RAM_CRIT, NET_WARN, NET_CRIT, NET_INTERFACE, NET_MAX_MB, NET_MIN_SCALE, NET_MAX_SCALE, NET_IDLE_THRESHOLD, NET_IDLE_RESET_TIME, LAUNCHERS, SELECTED_THEME, COLORS, FONT_FAMILY, FONT_SIZES
```
